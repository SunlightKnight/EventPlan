const padding = 18

export default {
  quarter: padding / 4,
  half: padding / 2,
  threequarters: 3 * padding / 4,
  full: padding,
  onehalf: padding * (3 / 2),
  double: padding * 2,
  triple: padding * 3,
  quadruple: padding * 4,
}